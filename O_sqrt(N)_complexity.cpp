#include<bits/stdc++.h>
using namespace std;
int main(){
    // O(sqrt(N)) complexity divisor ber kort use kora hoy.
    // O(sqrt(N)) complexity 1 sec e 10^14 step complte korte pare. sqrt(10^14)=10^7.
    return 0;
}